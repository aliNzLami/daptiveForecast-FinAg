name: Model Transitions

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  transitions:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install pandas numpy

      - name: Run transitions script
        run: python transition_points.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -f "dataset/transition_points.csv" ]; then
            git add dataset/transition_points.csv
            git commit -m "Add model transitions [skip ci]" || echo "No changes to commit"
            git push
          else
            echo "File dataset/transition_points.csv not found. Skipping commit."
            exit 1
          fi
